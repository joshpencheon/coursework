<% if @comment.new_record? %>
	$.facebox('<%= escape_javascript error_messages_for(:comment) %>')
<% else %>
	$('#comments').append("<%= escape_javascript render(:partial => 'comments/comment.html', :object => @comment) %>")
	$('.comment:last').hide().slideDown('slow')
<% end %>