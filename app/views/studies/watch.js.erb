var link = $('#watch_study_link')

link.attr('href','<%= escape_javascript(unwatch_study_path(@study)) %>')
link.fadeTo(150, 0.001, function(){
	link.text('Stop watching this study').fadeTo(150, 1)
})